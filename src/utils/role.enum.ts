export enum Role {
  User = 'user',
  Owner = 'owner',
}

export enum State {
  Active = 'active',
  Inactive = 'inactive',
}

// exercise force type
export enum ForceType {
  PUSH = 'push',
  PULL = 'pull',
  STATIC = 'static',
}

// difficulty of exercise
export enum ExerciseLevel {
  BEGINNER = 'beginner',
  INTERMEDIATE = 'intermediate',
  EXPERT = 'expert',
}
